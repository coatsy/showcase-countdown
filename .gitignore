# PlatformIO
.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Written to the project root and contains every -D flag in plaintext
compile_commands.json

# Python
__pycache__/
*.pyc

# Secrets
.env
.env.*
!.env.template
!.env.public

# Ignore the tmp folder, but keep the .keep file to make sure the folder is created on clone
tmp/*
!tmp/.keep

/serial-captures/

# Go server build output
server/showcase-server
server/showcase-server.exe
server/showcase-server.exe~
server/*.exe~
server/dist/
